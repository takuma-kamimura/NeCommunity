<%= turbo_frame_tag current_user do %>
<div class="flex items-center justify-center h-screen text-black">
    <% if @user.line_id.present? %>
      <%= link_to '連携したline_idを削除する', line_events_path(id: current_user.id), data: { turbo_method: :put, turbo_confirm: "本当に削除しますか？" }, class: "mt-4 bg-blue-500 text-white px-4 py-2 rounded" %>
    <% else %>
      <div class="flex flex-col items-center">
        <div class="text-3xl font-bold mb-4">友達追加してからLineでネコミュニティに登録してあるメースアドレスを送ってください</div>
        <img src="https://qr-official.line.me/gs/M_452ggkhv_GW.png?oat_content=qr" class="mt-4">
        <div class="mt-6"><%= link_to "Back",profile_path, class: 'border-black btn accent-color hover:bg-white text-black shadow-md md:text-lg text-sm whitespace-nowrap' %></div>
      </div>
    <% end %>
  </div>
<%end%>